{
  "version": "3.5.9",
  "released": "2026-06",
  "notes": "Fixes the backend 502: the Worker now forwards the ORIGINAL WebSocket upgrade request to the backend (fetch(target, request)) instead of rebuilding headers, so Cloudflare actually establishes the outbound socket. Combined with the port fix (backend must be on a Cloudflare-allowed port like 8080), backend mode now relays VLESS/VMess. Use /backend-test to verify.",
  "worker_url": "https://raw.githubusercontent.com/iiviirv/panel/main/worker.js"
}